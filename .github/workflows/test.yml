name: test-reusable
on:
  workflow_call:
    inputs:
      app:
        required: true
        type: string
      images:
        required: true
        type: string
    secrets:
      VPS_HOST:
        required: true
concurrency:
  group: t-${{ inputs.app }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        image: ${{ fromJSON(inputs.images) }}
    steps:
      - run: echo "suffix=${{ matrix.image.suffix }} host_set=${{ secrets.VPS_HOST != '' }}"
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy step"
